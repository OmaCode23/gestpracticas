/**
 * modules/empresas/components/EmpresaForm.tsx  —  Client Component
 *
 * Formulario de alta/edición de empresa.
 * Usa react-hook-form + zod para validación en tiempo real.
 *
 * Props:
 *  - onSuccess: callback que se ejecuta tras guardar correctamente,
 *               recibe la empresa creada/actualizada para que el
 *               componente padre pueda actualizar la lista.
 *  - defaultValues: si se pasa, activa el modo edición.
 */

