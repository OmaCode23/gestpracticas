/**
 * modules/formacion/components/FormacionForm.tsx  —  Client Component
 *
 * Formulario de alta de formación en empresa.
 * Nota: el campo "empresa" es un select dinámico que carga las
 * empresas desde la API. Se podría pasar como prop desde el
 * Server Component padre para evitar un fetch extra en el cliente.
 */

